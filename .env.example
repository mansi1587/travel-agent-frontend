# Where the FastAPI backend is running.
# Use "localhost" consistently — a browser treats localhost and 127.0.0.1 as different
# origins, so the auth cookie set by one is never sent to the other.
VITE_API_BASE_URL=http://localhost:8000
